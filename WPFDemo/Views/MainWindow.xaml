<Window x:Class="WPFDemo.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFDemo"
        xmlns:viewModels="clr-namespace:WPFDemo.ViewModels"
        xmlns:views="clr-namespace:WPFDemo.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <ResourceDictionary>
            <DataTemplate DataType="{x:Type viewModels:StateViewModel}">
                <views:StateView DataContext="{Binding}" />
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Label Content="Current State: " />
            <Label Width="100" HorizontalContentAlignment="Left">A1</Label>
            <ComboBox Width="150" Margin="5" />
            <Button Width="100" Margin="5" Click="Button_Click">Apply</Button>
        </StackPanel>

        <ContentPresenter x:Name="ContentPresenter" Width="100" Height="100" Grid.Row="1" />

    </Grid>
</Window>